# Напоминалкин 🤖

Telegram-бот для напоминаний с красивым меню, возможностью создавать напоминания себе и другим пользователям, а также ежедневные напоминания.

## 🎯 Функционал

- ✅ Создание напоминаний себе
- ✅ Создание напоминаний другим пользователям
- ✅ Ежедневные напоминания
- ✅ Просмотр и управление напоминаниями
- ✅ Красивое HTML форматирование сообщений
- ✅ Работа в Docker

## 🚀 Быстрый старт

### 1. Получите токен бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

### 2. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и укажите ваш токен:

```env
BOT_TOKEN=your_bot_token_here
TIMEZONE=Europe/Moscow
DATABASE_PATH=data/reminders.db
```

### 3. Запустите бота в Docker

```bash
docker-compose up --build
```

Бот запустится и будет готов к работе!

---

## 🐳 Docker

Этот проект изначально оптимизирован под запуск в Docker.

### Структура Docker

- `Dockerfile` — образ на базе `python:3.11-slim`
  - Устанавливается `tzdata`
  - Таймзона контейнера: `Europe/Moscow`
  - Устанавливаются зависимости из `requirements.txt`
  - Запускается `python -m bot.main`
- `docker-compose.yml` — один сервис `bot`:
  - том `./data:/app/data` для сохранения базы `SQLite`
  - переменные окружения загружаются из `.env`

### Основные команды

**Запуск с пересборкой образа:**

```bash
docker-compose up --build
```

**Запуск в фоне:**

```bash
docker-compose up -d
```

**Остановка контейнера:**

```bash
docker-compose down
```

**Перезапуск после изменения кода:**

```bash
docker-compose restart
```

### Логи бота

Посмотреть логи работающего контейнера:

```bash
docker-compose logs -f
```

Или только сервиса `bot`:

```bash
docker-compose logs -f bot
```

### Переменные окружения в Docker

Используются переменные из `.env`:

- `BOT_TOKEN` — токен бота от BotFather (обязательно)
- `TIMEZONE` — таймзона приложения (по умолчанию `Europe/Moscow`)
- `DATABASE_PATH` — путь к файлу БД внутри контейнера (по умолчанию `data/reminders.db`)

В `docker-compose.yml` дополнительно задаётся:

```yaml
environment:
  - TZ=Europe/Moscow
  - TIMEZONE=${TIMEZONE:-Europe/Moscow}
  - DATABASE_PATH=${DATABASE_PATH:-data/reminders.db}
```

Благодаря этому:

- системное время в контейнере — московское
- планировщик `APScheduler` и напоминания работают в МСК
- база данных хранится на хосте в папке `./data`

### Обновление образа после изменения кода

После изменения Python-кода или зависимостей:

```bash
docker-compose down
docker-compose up --build -d
```

Это пересоберёт образ и перезапустит бота с обновлённой логикой.

---

## 📋 Использование

### Команды

- `/start` — главное меню

### Создание напоминания себе

1. Нажмите "➕ Создать напоминание"
2. Введите текст напоминания
3. Укажите дату в формате `YYYY-MM-DD` (например: `2025-12-25`) или нажмите кнопку **Сегодня/Завтра**
4. Укажите время в формате `HH:MM` (например: `14:30`)
5. Для сегодняшней даты можно сразу нажать кнопку **Через 15 минут**

### Напоминание другому пользователю

1. Нажмите "👤 Напомнить другому"
2. Укажите `@username` или `user_id` получателя (надёжнее — `user_id`)
3. Введите текст напоминания
4. Укажите дату и время

### Ежедневное напоминание

1. Нажмите "🔁 Ежедневное напоминание"
2. Введите текст напоминания
3. Укажите время в формате `HH:MM`
4. Напоминание будет приходить каждый день в это время

### Просмотр напоминаний

1. Нажмите "📋 Мои напоминания"
2. Просмотрите список активных напоминаний
3. Нажмите на кнопку с напоминанием, чтобы удалить его

---

## 🏗️ Структура проекта

```text
napominalkin/
├── bot/
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── handlers/            # Обработчики команд
│   ├── keyboards/           # Inline клавиатуры
│   ├── states/              # FSM состояния
│   ├── services/            # Бизнес-логика
│   ├── database/            # Работа с БД
│   └── scheduler/           # Планировщик напоминаний
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 🛠️ Технологии

- **Python 3.11+**
- **aiogram 3.x** — фреймворк для Telegram ботов
- **APScheduler** — планировщик задач
- **SQLite** — база данных
- **Docker** — контейнеризация

## 📝 Переменные окружения

- `BOT_TOKEN` — токен бота (обязательно)
- `TIMEZONE` — таймзона (по умолчанию: `Europe/Moscow`)
- `DATABASE_PATH` — путь к файлу БД (по умолчанию: `data/reminders.db`)

## 🔧 Разработка

### Локальный запуск (без Docker)

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Создайте `.env` файл с токеном

3. Запустите бота:

```bash
python -m bot.main
```

## 📦 База данных

Бот использует SQLite для хранения данных. База данных создаётся автоматически при первом запуске.

**Таблицы:**
- `users` — информация о пользователях
- `reminders` — напоминания

## ⚠️ Важные замечания

- При перезапуске контейнера все активные напоминания сохраняются (БД на хосте в `./data`)
- Ежедневные напоминания автоматически планируются на следующий день
- Если пользователь заблокировал бота, отправитель получит уведомление об ошибке

## 📄 Лицензия

Этот проект создан для обучения и использования.
